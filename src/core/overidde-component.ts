import SDK from './sdk'

export function overideApp (
  sdk: SDK,
  config: Object
) {
  return config
}

export function overidePage (
  sdk: SDK,
  config: Object
) {
  return config
}

export function overideComponent (
  sdk: SDK,
  config: Object
) {
  return config
}